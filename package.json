{
  "name": "curriculum-system",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install:all": "npm install --prefix backend-worker && npm install --prefix frontend",
    "dev": "npm run dev --prefix frontend",
    "db:schema": "npx --prefix backend-worker wrangler d1 execute curriculum-db --remote --file=./schema.sql --yes",
    "db:seed": "npx --prefix backend-worker wrangler d1 execute curriculum-db --remote --file=./seed.sql --yes",
    "db:init": "npm run db:schema && npm run db:seed",
    "deploy:backend": "npx --prefix backend-worker wrangler deploy",
    "build": "npm run build --prefix frontend"
  }
}
